buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath("org.sonarsource.scanner.gradle:sonarqube-gradle-plugin:2.8")
    }
}

subprojects {
    apply plugin: "java"

    group = "org.arobase"
    version = "1.0.0-SNAPSHOT"
}